# .air.toml
root = "."
tmp_dir = "tmp"

[log]
  time = true

[build]
  cmd = "go build -o main ."
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["assets", "tmp", "vendor"]
  bin = "main"
